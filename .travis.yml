language: node_js
node_js:
  - "10.16.3"

sudo: true
dist: bionic

addons:
  apt:
    packages:
      - firefox

install:
  - npm install
  - mv test/config.travis.json test/config.json

script:
  - npm test
